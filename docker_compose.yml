services:
  gatekeeper:
    container_name: gatekeeper
    build: .
    command:
      - sh
      - -c
      - "python manage.py migrate && python manage.py runserver 0.0.0.0:${SERVER_PORT}"
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /var/lib/gatekeeper:/var/lib/gatekeeper
      - gatekeeper_data:/app/data

volumes:
  gatekeeper_data: